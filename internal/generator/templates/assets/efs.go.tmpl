package assets

import "embed"

// Files embeds all static assets for production builds
// In development, files are served from disk for hot reload
//
//go:embed <!-- CSS_EMBED_PATH -->
var Files embed.FS
